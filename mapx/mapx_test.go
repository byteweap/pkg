package mapx

import (
	"testing"
)

func TestMapx(t *testing.T) {

	// string - int
	// mint := New[string, int]()
	// mint.Set("name", 1)
	// t.Log(mint.Get("name"))
	//

	a := make(KMap[string], 0)
	a["ass"] = 7
}
